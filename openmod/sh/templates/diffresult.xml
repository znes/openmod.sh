<?xml version="1.0" encoding="UTF-8"?>
<diffResult generator="openmod.sh" version="0.6">
{% for m in modifications %}
  <{{m.tag}} old_id="{{m.old_id}}" new_id="{{m.new_id}}"
             new_version="{{m.version}}" />
{% endfor %}
</diffResult>
